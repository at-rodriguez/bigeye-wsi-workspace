---
source_name: snowflake_warehouse
table_name: TOOY_DSTBTN_ORDER_ID
sql: |
  SELECT ORDER_ID, CONCAT(CUSTOMER_ID,'-',SHIPPING_ZIP) AS COMPOSITE_ATTRIBUTE
  FROM TOOY_DEMO.PROD_REPL.ORDERS
  GROUP BY 1,2